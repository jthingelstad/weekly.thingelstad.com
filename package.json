{
  "name": "weekly-thing-site",
  "version": "1.0.0",
  "description": "The Weekly Thing — newsletter landing page and archive",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "pagefind": "pagefind --site _site --glob '**/*.html'"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "markdown-it-anchor": "^9.2.0",
    "pagefind": "^1.3.0"
  }
}
